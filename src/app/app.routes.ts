import { Routes } from '@angular/router';
import { SidelinesLoginComponent } from './sidelines-login/sidelines-login.component';
 

 
export const appRoutes: Routes = [
  { path: '', component: SidelinesLoginComponent },

];
 